{
  "name": "your-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --scope container --scope header --scope app2 --scope login"
  }
}
